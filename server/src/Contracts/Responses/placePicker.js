class GetPlacesResponse {
  constructor({ rows, columns, seats, }) {
    this.rows = rows;
    this.columns = columns;
    this.rows = rows;
    this.seats = seats;
  }
}

module.exports = {
  GetPlacesResponse,
};
